{
  "name": "alles-standard-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.5.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
